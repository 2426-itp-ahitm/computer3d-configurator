export interface Gpu {
    gpu_id: number
    name: string
    price: number
    chipset: string
    memory: number
    core_clock: number
    boost_clock: number
    color: string
    length: number
    img: string
}
